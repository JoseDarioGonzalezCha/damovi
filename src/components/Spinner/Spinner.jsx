import React from "react";
import { Loader, SpinnerContainer } from "./Spinner.Style";

export const Spinner = () => {
  return (
    <SpinnerContainer>
      <Loader></Loader>
    </SpinnerContainer>
  );
};
